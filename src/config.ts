/**
 * App configuration.
 *
 * Before running the app, replace the Mapbox token below with your own.
 * Get a free token at: https://account.mapbox.com/access-tokens/
 */
export const config = {
  mapbox: {
    accessToken: 'YOUR_MAPBOX_ACCESS_TOKEN',
  },
} as const;
